name: sync-release
on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

permissions:
  contents: write

env:
  UPSTREAM: LiteLDev/Levilamina
  STATE_FILE: .release-cache/synced_tag.txt

jobs:
  sync:
    runs-on: ubuntu-latest
    concurrency:
      group: sync-release
    steps:
      - uses: actions/checkout@v7

      - name: Get Levilamina latest release
        id: upstream
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TAG=$(gh release view --repo "$UPSTREAM" --json tagName -q .tagName)
          echo "tag=$TAG" >> "$GITHUB_OUTPUT"

      - name: Load last tag
        id: state
        run: |
          if [ -f "$STATE_FILE" ]; then
            LAST=$(cat "$STATE_FILE")
          else
            LAST=""
          fi
          echo "last=$LAST" >> "$GITHUB_OUTPUT"

      - name: Create release
        if: steps.upstream.outputs.tag != '' && steps.upstream.outputs.tag != steps.state.outputs.last
        env:
          GH_TOKEN: ${{ secrets.MYTOKEN }}
          TAG: ${{ steps.upstream.outputs.tag }}
          UPSTREAM: ${{ env.UPSTREAM }}
        run: |
          TITLE=$(gh release view "$TAG" --repo "$UPSTREAM" --json name -q .name)
          BODY=$(gh release view "$TAG" --repo "$UPSTREAM" --json body -q .body)

          if gh release view "$TAG" --repo "$GITHUB_REPOSITORY" >/dev/null 2>&1; then
            echo "B already has $TAG, skip"
          else
            gh release create "$TAG" \
              --repo "$GITHUB_REPOSITORY" \
              --title "${TITLE:-$TAG}" \
              --notes "${BODY:-Synced from $UPSTREAM}"
          fi

          mkdir -p "$(dirname "$STATE_FILE")"
          echo "$TAG" > "$STATE_FILE"

      - name: Commit state file
        if: steps.upstream.outputs.tag != '' && steps.upstream.outputs.tag != steps.state.outputs.last
        run: |
          git config user.name "wed150"
          git config user.email "wed150@users.noreply.github.com"
          git add "$STATE_FILE"
          git commit -m "chore: update release state to ${{ steps.upstream.outputs.tag }}" || true
          git push
