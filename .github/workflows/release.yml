on:
  release:
    types:
      - published

permissions:
  contents: write

env:
  UPSTREAM: LiteLDev/LeviLamina
  UPSTREAM_URL: https://github.com/LiteLDev/LeviLamina.git

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        target_type:
          - server
          - client
        mode:
          - debug
          - release
    runs-on: windows-latest
    steps:
      - name: Clone repo at release tag
        run: |
          git clone ${{ env.UPSTREAM_URL }} upstream
          cd upstream
          git checkout ${{ github.event.release.tag_name }}

      - uses: xmake-io/github-action-setup-xmake@v1

      - uses: actions/cache@v6
        with:
          path: |
            ~\AppData\Local\.xmake
          key: xmake-${{ hashFiles('upstream/xmake.lua') }}
          restore-keys: |
            xmake-

      - uses: microsoft/setup-msbuild@v3

      - run: |
          xmake repo -u

      - run: |
          xmake f -a x64 -m ${{ matrix.mode }} -p windows -v -y --target_type=${{ matrix.target_type }} --publish=y
        working-directory: upstream

      - run: |
          xmake -v -y
        working-directory: upstream

      - run: |
          xmake install -o ./sdk
        working-directory: upstream

      - uses: actions/upload-artifact@v7
        with:
          name: levilamina-lib-${{ github.event.release.tag_name }}-${{ matrix.target_type }}-${{ matrix.mode }}-windows-x64
          path: |
            upstream/sdk/


  upload-include:
    needs:
      - build
    runs-on: ubuntu-latest
    strategy:
      matrix:
        target_type:
          - server
          - client
    steps:
      - name: Checkout Levilamina-lib repo
        uses: actions/checkout@v7
        with:
          token: ${{ secrets.MYTOKEN }}
          fetch-depth: 0

      - uses: actions/download-artifact@v8
        with:
          name: levilamina-lib-${{ github.event.release.tag_name }}-${{ matrix.target_type }}-release-windows-x64
          path: _artifact

      - name: Commit include files to repo
        env:
          VERSION: ${{ github.event.release.tag_name }}
          TOKEN: ${{ secrets.MYTOKEN }}
        run: |
          set -e
          numVERSION="${VERSION#v}"
          SRC="_artifact/include"
          DST="include/${numVERSION}/${{ matrix.target_type }}"

          echo "=== Source: $SRC ==="
          echo "=== Dest:   $DST ==="

          if [ ! -d "$SRC" ]; then
            echo "ERROR: include not found at $SRC"
            echo "--- _artifact contents ---"
            find _artifact -maxdepth 3 -type d
            exit 1
          fi

          mkdir -p "$DST"
          rm -rf "$DST"/*
          cp -r "$SRC"/. "$DST"/

          FILE_COUNT=$(find "$DST" -type f | wc -l)
          echo "=== Committed $FILE_COUNT header files ==="

          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git add "include/${numVERSION}/${{ matrix.target_type }}/"

          if git diff --cached --quiet; then
            echo "=== No include changes for ${{ matrix.target_type }} ==="
          else
            git commit -m "feat(include): add ${numVERSION} ${{ matrix.target_type }} headers"
            git push https://${TOKEN}@github.com/wed150/Levilamina-lib.git
            echo "=== Include pushed for ${{ matrix.target_type }} ==="
          fi

      - name: Cleanup
        if: always()
        run: rm -rf _artifact

  upload-to-release:
    needs:
      - build
    runs-on: ubuntu-latest
    strategy:
      matrix:
        target_type:
          - server
          - client
        mode:
          - debug
          - release
    steps:
      - uses: actions/download-artifact@v8
        with:
          name: levilamina-lib-${{ github.event.release.tag_name }}-${{ matrix.target_type }}-${{ matrix.mode }}-windows-x64
          path: artifact

      - run: |
          cd artifact
          rm -rf include
          zip -1 -r ../levilamina-lib-${{ github.event.release.tag_name }}-${{ matrix.target_type }}-${{ matrix.mode }}-windows-x64.zip *
        shell: bash

      - uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ github.event.release.tag_name }}
          files: |
            levilamina-lib-${{ github.event.release.tag_name }}-${{ matrix.target_type }}-${{ matrix.mode }}-windows-x64.zip

  report-to-xmake-repo:
    needs:
      - upload-to-release
      - upload-include
    runs-on: ubuntu-latest
    steps:
      - name: Checkout xmake-repo (target)
        uses: actions/checkout@v7
        with:
          repository: wed150/xmake-repo
          path: xmake-repo
          token: ${{ secrets.MYTOKEN }}
          fetch-depth: 0

      - name: Checkout upstream xmake-repo
        uses: actions/checkout@v7
        with:
          repository: LiteLDev/xmake-repo
          path: upstream-xmake-repo
          fetch-depth: 1

      - name: Configure git
        working-directory: xmake-repo
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Copy versions & compute sha256
        working-directory: xmake-repo
        env:
          VERSION: ${{ github.event.release.tag_name }}
          REPO: ${{ github.repository }}
          TOKEN: ${{ secrets.MYTOKEN }}
        run: |
          set -e
          numVERSION="${VERSION#v}"

          SRC="../upstream-xmake-repo/packages/l/levilamina/versions"
          DST="packages/l/levilamina-lib/versions"
          mkdir -p "$DST"

          echo "=== Copying versions from upstream ==="
          if [ -d "$SRC" ]; then
            for f in "$SRC"/*; do
              [ -f "$f" ] || continue
              bf=$(basename "$f")
              cp "$f" "$DST/$bf"
              echo "Copied: $bf"
            done
          fi

          PREBUILT_FILE="packages/l/levilamina-lib/versions/prebuilt_versions.txt"
          touch "$PREBUILT_FILE"
          TMPFILE=$(mktemp)

          for TARGET in server client; do
            for MODE in release debug; do
              FILENAME="levilamina-lib-${numVERSION}-${TARGET}-${MODE}-windows-x64.zip"
              DOWNLOAD_URL="https://github.com/${REPO}/releases/download/${VERSION}/${FILENAME}"
              KEY="${numVERSION}-windows-${MODE}-${TARGET}"

              echo "=== Processing ${KEY} ==="
              curl -L -o sdk.zip "$DOWNLOAD_URL"
              SHA256=$(sha256sum sdk.zip | cut -d' ' -f1)
              rm -f sdk.zip

              if grep -q "^${KEY} " "$PREBUILT_FILE" 2>/dev/null; then
                echo "Entry $KEY already exists, skipping"
                continue
              fi

              echo "${KEY} ${SHA256}" >> "$TMPFILE"
              echo "Queued: ${KEY} ${SHA256}"
            done
          done

          git add packages/l/levilamina-lib/versions/

          if [ -s "$TMPFILE" ]; then
            cat "$TMPFILE" >> "$PREBUILT_FILE"
            git add "$PREBUILT_FILE"
          fi

          if git diff --cached --quiet; then
            echo "=== Nothing new to commit ==="
          else
            echo "=== Committing ==="
            [ -s "$TMPFILE" ] && cat "$TMPFILE"
            git commit -m "feat(levilamina-lib): add prebuilt sha for ${numVERSION} & sync versions"
            git push https://${TOKEN}@github.com/wed150/xmake-repo.git
            echo "=== Push done ==="
          fi

          rm -f "$TMPFILE"
          echo "=== Final prebuilt_versions.txt ==="
          cat "$PREBUILT_FILE"
